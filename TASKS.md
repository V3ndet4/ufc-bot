# Build Tasks

1. Add a real odds provider client in `data_sources/odds_api.py`.
2. Expand SQLite persistence to support open and closing line comparisons.
3. Add fighter history ingestion and richer feature generation.
4. Replace heuristic probabilities with trained model outputs.
5. Compare open, current, and closing lines.
6. Add bankroll constraints by card and by market type.
7. Expand backtesting to measure ROI, CLV, hit rate, and drawdown by market.
8. Keep auto-bet placement out of scope until the data and model are trustworthy.
